export enum LOG_LEVEL {
  INFO = "info",
  ERROR = "error",
  DEBUG = "debug",
  FATAL = "fatal",
}

export enum APP_ENV {
  DEVELOPMENT = "development",
  STAGING = "staging",
  PRODUCTION = "production",
}
